#[include(*Plugins, *Operators)]
// new_oper?@('??', 1, 1, 71, { if?@(lhs?, rhs?.0, rhs?.1)! })!;
// new_oper?@('::', 1, 1, 70, { [lhs?, rhs?]! })!;

// io = import@('/Users/westerhack/code/rust/qutie_rust/examples/io.qt')!;
// example = io.read_url@0(url = 'www.example.com')!;
// disp@(example)!

#[include(AutoDeref, AutoFunctionCall)]
foo = {
  bar = {
    a + b
  };
}!;
foo.bar(a=3; b=4;).0