#[include(*Builtins, *Operators, *Plugins)]

greet = {
  #[include(disp, AutoDeref, AutoFunctionCall, +, Text, Universe)]
  disp("Hello, " + name)
  #[exclude(disp, AutoDeref, AutoFunctionCall, +, Text, Universe)]
};

// get_pwd = {
//   #[include(syscall, AutoDeref, AutoFunctionCall, Text, Universe)]
//   syscall('pwd')
// };